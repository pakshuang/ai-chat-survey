# NETWORKING
NGINX_CONTAINER_PORT=80 #80 in development, 443 in production
NGINX_HOST_PORT=80 #80 in development, 443 in production
BACKEND_CONTAINER_PORT=5000
FRONTEND_CONTAINER_PORT=5173
DOMAIN=localhost
CERT_EMAIL=dev@example.com #email for Let's Encrypt
VITE_BASE_URL=http://${DOMAIN}/api/v1

# REVERSE PROXY
NGINX_ENV=development

# FRONTEND
NODE_ENV=development

# BACKEND
FLASK_ENV=development
FLASK_SECRET_KEY=default_key_for_development

# DATABASE
MYSQL_CHARSET=utf8mb4
MYSQL_COLLATION=utf8mb4_unicode_ci
MYSQL_DATABASE=ai_chat_survey_db
MYSQL_INITDB_SKIP_CHARSET=yes
MYSQL_INITDB_SKIP_TZINFO=yes
MYSQL_ROOT_PASSWORD=password
API_MYSQL_HOST=database #must correspond with database service name in compose.yaml
API_MYSQL_USER=root
API_MYSQL_PASSWORD=password
API_MYSQL_DB=ai_chat_survey_db
TZ=Asia/Singapore

# SECRETS
OPENAI_API_KEY=